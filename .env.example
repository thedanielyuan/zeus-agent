# Copy to .env.local (gitignored). Never commit real keys.

# Required for the Anthropic provider (Claude Sonnet 5).
ANTHROPIC_API_KEY=

# Optional.
# ZEUS_DB_PATH=./data/zeus.db
# ZEUS_LOG_LEVEL=info
